import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MediConnect smoke test', (WidgetTester tester) async {
    // Basic smoke test — full integration test requires device
    expect(true, isTrue);
  });
}
